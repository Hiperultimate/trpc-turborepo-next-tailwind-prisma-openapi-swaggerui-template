{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "dotenv -- turbo run dev",
    "prisma": "dotenv -- yarn workspace @template/prisma",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint": "^8.47.0",
    "prettier": "^3.0.2",
    "prettier-config": "*",
    "tsconfig": "*",
    "turbo": "latest",
    "dotenv-cli": "^7.3.0"
  },
  "name": "trpc-server-client-turborepo-template",
  "packageManager": "yarn@1.22.19",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
